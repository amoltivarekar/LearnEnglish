<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amolt.learnenglish">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".GrammerActivity"
            android:label="@string/title_activity_grammer"
            android:theme="@style/AppTheme" />
        <activity android:name=".GrammerTab1Activity" />
        <activity android:name=".GrammerTab2Activity" />
        <activity android:name=".GrammerTab3Activity" />
        <activity
            android:name=".ReadingActivity"
            android:label="@string/title_activity_reading"
            android:theme="@style/AppTheme" />
        <activity android:name=".ReadingTab1Activity" />
        <activity android:name=".ReadingTab1Activity1" />
        <activity
            android:name=".VideoActivity"
            android:label="@string/title_activity_video"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>